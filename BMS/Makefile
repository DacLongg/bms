##########################################################################################################################
# File automatically-generated by tool: [projectgenerator] version: [4.8.0-B50] date: [Fri May 22 19:04:22 ICT 2026] 
##########################################################################################################################

# ------------------------------------------------
# Generic Makefile (based on gcc)
#
# ChangeLog :
#	2017-02-10 - Several enhancements + project update mode
#   2015-07-22 - first version
# ------------------------------------------------

######################################
# target
######################################
TARGET = BMS


######################################
# building variables
######################################
# debug build?
DEBUG = 1
# optimization
OPT = -Os


#######################################
# paths
#######################################
# Build path
BUILD_DIR = build
BUILD_CONFIG_FILE = $(BUILD_DIR)/.build_config

######################################
# source
######################################
# C sources
C_SOURCES =  \
Core/Src/main.c \
Core/Src/gpio.c \
Core/Src/adc.c \
Core/Src/lptim.c \
Core/Src/rtc.c \
Core/Src/usart.c \
Core/Src/stm32l0xx_it.c \
Core/Src/stm32l0xx_hal_msp.c \
App/mainapp.c \
MyMiddlewares/bms/bms.c \
MyDrivers/power/power_manager.c \
MyDrivers/bq76952/bq76952.c \
MyDrivers/i2c_soft/i2c_soft.c \
MyDrivers/bms_uart/bms_uart.c \
MyDrivers/debug_log/debug_log.c \
MyDrivers/storage_flash/storage_flash.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_adc.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_adc_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_i2c.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_i2c_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_rcc.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_rcc_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_flash_ramfunc.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_flash.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_flash_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_gpio.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_dma.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_pwr.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_pwr_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_cortex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_exti.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_lptim.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_rtc.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_rtc_ex.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_uart.c \
Drivers/STM32L0xx_HAL_Driver/Src/stm32l0xx_hal_uart_ex.c \
Core/Src/system_stm32l0xx.c \
Core/Src/sysmem.c \
Core/Src/syscalls.c

# ASM sources
ASM_SOURCES =  \
startup_stm32l010x6.s

# ASM sources
ASMM_SOURCES = 


#######################################
# binaries
#######################################
PREFIX = arm-none-eabi-
# The gcc compiler bin path can be either defined in make command via GCC_PATH variable (> make GCC_PATH=xxx)
# either it can be added to the PATH environment variable.
ifdef GCC_PATH
CC = $(GCC_PATH)/$(PREFIX)gcc
AS = $(GCC_PATH)/$(PREFIX)gcc -x assembler-with-cpp
CP = $(GCC_PATH)/$(PREFIX)objcopy
SZ = $(GCC_PATH)/$(PREFIX)size
else
CC = $(PREFIX)gcc
AS = $(PREFIX)gcc -x assembler-with-cpp
CP = $(PREFIX)objcopy
SZ = $(PREFIX)size
endif
HEX = $(CP) -O ihex
BIN = $(CP) -O binary -S
ST_FLASH ?= st-flash
OPENOCD ?= openocd
GDB ?= arm-none-eabi-gdb
FLASH_ADDR ?= 0x08000000
GDB_PORT ?= 3333
OPENOCD_INTERFACE ?= interface/stlink.cfg
OPENOCD_TARGET ?= target/stm32l0.cfg
 
#######################################
# CFLAGS
#######################################
# cpu
CPU = -mcpu=cortex-m0plus

# fpu
# NONE for Cortex-M0/M0+/M3

# float-abi


# mcu
MCU = $(CPU) -mthumb $(FPU) $(FLOAT-ABI)

# macros for gcc
# AS defines
AS_DEFS = 

# C defines
C_DEFS =  \
-DUSE_HAL_DRIVER \
-DSTM32L010x6


# AS includes
AS_INCLUDES = 

# C includes
C_INCLUDES =  \
-I. \
-ICore/Inc \
-IMyMiddlewares/bms \
-IMyDrivers/bq76952 \
-IMyDrivers/i2c_soft \
-IMyDrivers/bms_uart \
-IMyDrivers/power \
-IMyDrivers/debug_log \
-IMyDrivers/storage_flash \
-IDrivers/STM32L0xx_HAL_Driver/Inc \
-IDrivers/STM32L0xx_HAL_Driver/Inc/Legacy \
-IDrivers/CMSIS/Device/ST/STM32L0xx/Include \
-IDrivers/CMSIS/Include


# compile gcc flags
ASFLAGS = $(MCU) $(AS_DEFS) $(AS_INCLUDES) $(OPT) -Wall -fdata-sections -ffunction-sections

CFLAGS += $(MCU) $(C_DEFS) $(C_INCLUDES) $(OPT) -Wall -fdata-sections -ffunction-sections
USER_DEFS ?=
CFLAGS += $(USER_DEFS)

ifeq ($(DEBUG), 1)
CFLAGS += -g -gdwarf-2
endif


# Generate dependency information
CFLAGS += -MMD -MP -MF"$(@:%.o=%.d)"


#######################################
# LDFLAGS
#######################################
# link script
LDSCRIPT = STM32L010XX_FLASH.ld

# libraries
LIBS = -lc -lm -lnosys 
LIBDIR = 
LDFLAGS = $(MCU) -specs=nano.specs -T$(LDSCRIPT) $(LIBDIR) $(LIBS) -Wl,-Map=$(BUILD_DIR)/$(TARGET).map,--cref -Wl,--gc-sections

# default action: build all
all: $(BUILD_DIR)/$(TARGET).elf $(BUILD_DIR)/$(TARGET).hex $(BUILD_DIR)/$(TARGET).bin


#######################################
# build the application
#######################################
# list of objects
OBJECTS = $(addprefix $(BUILD_DIR)/,$(notdir $(C_SOURCES:.c=.o)))
vpath %.c $(sort $(dir $(C_SOURCES)))
# list of ASM program objects
OBJECTS += $(addprefix $(BUILD_DIR)/,$(notdir $(ASM_SOURCES:.s=.o)))
vpath %.s $(sort $(dir $(ASM_SOURCES)))
OBJECTS += $(addprefix $(BUILD_DIR)/,$(notdir $(ASMM_SOURCES:.S=.o)))
vpath %.S $(sort $(dir $(ASMM_SOURCES)))

BUILD_CONFIG_VARS = $(MCU) $(C_DEFS) $(C_INCLUDES) $(AS_DEFS) $(AS_INCLUDES) $(OPT) DEBUG=$(DEBUG) USER_DEFS=$(USER_DEFS)

.PHONY: FORCE

$(BUILD_CONFIG_FILE): FORCE | $(BUILD_DIR)
	@printf '%s\n' '$(BUILD_CONFIG_VARS)' > $(BUILD_DIR)/.build_config.tmp
	@cmp -s $(BUILD_DIR)/.build_config.tmp $@ || mv $(BUILD_DIR)/.build_config.tmp $@
	@rm -f $(BUILD_DIR)/.build_config.tmp

$(BUILD_DIR)/%.o: %.c Makefile $(BUILD_CONFIG_FILE) | $(BUILD_DIR) 
	$(CC) -c $(CFLAGS) -Wa,-a,-ad,-alms=$(BUILD_DIR)/$(notdir $(<:.c=.lst)) $< -o $@

$(BUILD_DIR)/%.o: %.s Makefile $(BUILD_CONFIG_FILE) | $(BUILD_DIR)
	$(AS) -c $(CFLAGS) $< -o $@
$(BUILD_DIR)/%.o: %.S Makefile $(BUILD_CONFIG_FILE) | $(BUILD_DIR)
	$(AS) -c $(CFLAGS) $< -o $@

$(BUILD_DIR)/$(TARGET).elf: $(OBJECTS) Makefile
	$(CC) $(OBJECTS) $(LDFLAGS) -o $@
	$(SZ) $@

$(BUILD_DIR)/%.hex: $(BUILD_DIR)/%.elf | $(BUILD_DIR)
	$(HEX) $< $@
	
$(BUILD_DIR)/%.bin: $(BUILD_DIR)/%.elf | $(BUILD_DIR)
	$(BIN) $< $@	
	
$(BUILD_DIR):
	mkdir $@		

.PHONY: all clean flash flash-auto flash-stlink flash-openocd debug-server debug erase check-gdb check-openocd check-stflash

check-stflash:
	@command -v $(ST_FLASH) >/dev/null 2>&1 || { echo "Missing $(ST_FLASH). Install stlink-tools."; exit 1; }

check-openocd:
	@command -v $(OPENOCD) >/dev/null 2>&1 || { echo "Missing $(OPENOCD). Install openocd."; exit 1; }

check-gdb:
	@command -v $(GDB) >/dev/null 2>&1 || { echo "Missing $(GDB). Install gcc-arm-none-eabi or gdb-multiarch."; exit 1; }

flash: flash-auto

flash-auto: $(BUILD_DIR)/$(TARGET).elf $(BUILD_DIR)/$(TARGET).bin
	@if command -v $(ST_FLASH) >/dev/null 2>&1; then \
		echo "Flashing with $(ST_FLASH)"; \
		$(ST_FLASH) --reset write $(BUILD_DIR)/$(TARGET).bin $(FLASH_ADDR); \
	elif command -v $(OPENOCD) >/dev/null 2>&1; then \
		echo "Flashing with $(OPENOCD)"; \
		$(OPENOCD) -f $(OPENOCD_INTERFACE) -f $(OPENOCD_TARGET) -c "program $(BUILD_DIR)/$(TARGET).elf verify reset exit"; \
	else \
		echo "Missing flash tool. Install stlink-tools or openocd."; \
		exit 1; \
	fi

flash-stlink: $(BUILD_DIR)/$(TARGET).bin check-stflash
	$(ST_FLASH) --reset write $(BUILD_DIR)/$(TARGET).bin $(FLASH_ADDR)

flash-openocd: $(BUILD_DIR)/$(TARGET).elf check-openocd
	$(OPENOCD) -f $(OPENOCD_INTERFACE) -f $(OPENOCD_TARGET) -c "program $(BUILD_DIR)/$(TARGET).elf verify reset exit"

erase: check-stflash
	$(ST_FLASH) erase

debug-server: check-openocd
	$(OPENOCD) -f $(OPENOCD_INTERFACE) -f $(OPENOCD_TARGET)

debug: $(BUILD_DIR)/$(TARGET).elf check-gdb
	@echo "Start 'make debug-server' in another terminal first."
	$(GDB) $(BUILD_DIR)/$(TARGET).elf -ex "target extended-remote localhost:$(GDB_PORT)" -ex "monitor reset halt" -ex "load" -ex "monitor reset halt"

#######################################
# clean up
#######################################
clean:
	-rm -fR $(BUILD_DIR)
  
#######################################
# dependencies
#######################################
-include $(wildcard $(BUILD_DIR)/*.d)

# *** EOF ***
